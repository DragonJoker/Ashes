struct PixelInput
{
	float uv : TEXCOORD;
};

tbuffer mapColour : register( t0 )
{
	float4 colour[512];
}

float4 mainPx( in PixelInput input ) : SV_TARGET
{
	return colour[int( clamp( input.uv * 512, 0.0, 511.0 ) )];
}
